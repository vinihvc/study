---
title: Contains Duplicate
excerpt: Check if array contains any duplicate values
description: Given an integer array, determine if any value appears at least twice in the array.
difficulty: easy
solved: false
starter: |
  const containsDuplicate = (nums) => {
    // Your implementation here
  };

  console.log(containsDuplicate([1, 2, 3, 1]));
tests:
  - name: Input 1
    code: containsDuplicate([1, 2, 3, 1])
    expectedInput: { nums: [1, 2, 3, 1] }
    expectedResult: true
  - name: Input 2
    code: containsDuplicate([1, 2, 3, 4])
    expectedInput: { nums: [1, 2, 3, 4] }
    expectedResult: false
  - name: Input 3
    code: containsDuplicate([1, 1, 1, 3, 3, 4, 3, 2, 4, 2])
    expectedInput: { nums: [1, 1, 1, 3, 3, 4, 3, 2, 4, 2] }
    expectedResult: true
---

Write a function that returns `true` if any value appears at least twice in the array, and `false` if every element is distinct.

## How to Solve

<Spoiler>

Use a Set to track seen numbers. As you iterate through the array, if a number is already in the set, return true. Otherwise, add it to the set. If you finish iterating without finding duplicates, return false.

</Spoiler>
